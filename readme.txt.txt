Classification
0 : good
1 : bad

Images stored as: path+'/Train/Good/' and path+'/Train/Bad/'

run Detect_Defect_Train <image_folder_path> for training
run Detect_Defect_Test <image_path> <model_path> for prediction

the _optimized model is optimized using tensorflow transform_graph.exe
